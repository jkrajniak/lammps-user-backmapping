# backmap-prep

Generate LAMMPS input files for CG-to-AT backmapping simulations.

## Installation

```bash
pip install -e .
```

## Usage

```bash
backmap-prep settings.yaml
```

See the parent repository's `openspec/` directory for full specification.
